{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*",
    "www"
  ],
  "scripts": {
    "dev": "npx lerna exec --scope=www npm run dev",
    "runserver": "mvnw spring-boot:run -pl server"
  },
  "devDependencies": {
    "lerna": "^5.6.2"
  }
}
